import {SafeAreaView, ScrollView, Text, View} from 'react-native';
import React from 'react';

function ExperienceDetails() {
    return (
        <SafeAreaView>
            <ScrollView>
                <View>
                    <Text>ExperienceDetails</Text>
                </View>
            </ScrollView>
        </SafeAreaView>
    );
}

export default ExperienceDetails;
